<?xml version="1.0" encoding="UTF-8"?>
<!-- Replaces the image's password-authn-config.xml. Validates the username /
     password against the static htpasswd file (bind-mounted) using the
     HTPasswdCredentialValidator (requires Shibboleth IdP >= 4.1). The htpasswd
     file holds the single ${DEMO_USER} account written by scripts/gen-certs.sh. -->
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:util="http://www.springframework.org/schema/util"
       xmlns:p="http://www.springframework.org/schema/p"
       xmlns:c="http://www.springframework.org/schema/c"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
                           http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd
                           http://www.springframework.org/schema/util http://www.springframework.org/schema/util/spring-util.xsd"
       default-init-method="initialize"
       default-destroy-method="destroy">

    <util:list id="shibboleth.authn.Password.Validators">
        <ref bean="htpasswdValidator"/>
    </util:list>

    <bean id="htpasswdValidator" parent="shibboleth.HTPasswdValidator"
          p:resource="%{idp.home}/credentials/idp.htpasswd"/>

</beans>
