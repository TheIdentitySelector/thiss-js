<!DOCTYPE html>
<!-- Rendered from index.html.tmpl by scripts/render.sh. Public (unauthenticated)
     landing page for SP1 — "Andaliz Publishing". Visitors see the abstract + a
     truncated introduction and the SeamlessAccess button; full text is behind
     login (see secure/). The blue button is rendered by thiss.js into #login —
     do not replace it with a static look-alike. -->
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Andaliz Publishing — Hydrostatic Equilibrium and the Near-Spherical Structure of the Sun</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Noto+Sans:wght@300;400;600;700&family=Roboto:wght@400;500;600&family=Inter:wght@400;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --pastel: #f9eaff; --purple: #7f57f1; --ink: #333; --head: #222;
            --muted: #666; --border: #e9e9e9; --avatar: #b3a0d5;
        }
        * { box-sizing: border-box; }
        body {
            margin: 0; color: var(--ink); background: #fff;
            font-family: "Noto Sans", system-ui, sans-serif; line-height: 1.5;
        }
        a { color: var(--purple); }
        .container { max-width: 1140px; margin: 0 auto; padding: 0 24px; }

        .site-header { background: var(--pastel); }
        .header-inner { display: flex; align-items: center; gap: 14px; height: 84px; }
        .brand-mark { width: 36px; height: 36px; display: block; }
        .brand-name { font-family: Roboto, "Noto Sans", sans-serif; font-weight: 600; font-size: 20px; color: #000; }

        .layout { display: grid; grid-template-columns: minmax(0, 1fr) 320px; gap: 56px; padding-top: 48px; padding-bottom: 64px; }
        @media (max-width: 880px) { .layout { grid-template-columns: 1fr; gap: 40px; } }

        .title { font-weight: 600; font-size: 27px; line-height: 1.4; color: var(--purple); margin: 0 0 28px; }

        .access-row { display: flex; flex-wrap: wrap; align-items: center; gap: 8px 16px; margin-bottom: 40px; }
        #login { width: 350px; max-width: 100%; min-height: 58px; }
        .alt-access { margin: 0; font-size: 15px; color: #000; }

        .tab {
            display: inline-flex; align-items: center; background: var(--pastel);
            padding: 12px 30px; border-radius: 8px 8px 0 0; font-size: 15px; color: var(--ink);
        }
        .abstract { background: var(--pastel); border-radius: 0 8px 8px 8px; padding: 28px 32px; margin: 0 0 44px; }
        .abstract p { margin: 0 0 14px; font-size: 15px; color: var(--muted); }
        .abstract p:last-child { margin-bottom: 0; }

        .section-title { font-weight: 600; font-size: 21px; color: var(--ink); margin: 0 0 12px; }
        .body p { font-size: 15px; color: var(--muted); margin: 0 0 16px; }

        /* Truncated/locked introduction: fade the lower portion out. */
        .locked { position: relative; max-height: 280px; overflow: hidden; }
        .locked::after {
            content: ""; position: absolute; inset: auto 0 0 0; height: 150px;
            background: linear-gradient(rgba(255,255,255,0), #fff);
        }
        .cta {
            background: var(--pastel); border-radius: 8px; text-align: center;
            padding: 22px 24px; margin: 28px auto 0; max-width: 660px;
        }
        .cta-title { font-weight: 700; font-size: 15px; color: #000; margin: 0 0 4px; }
        .cta p { margin: 0; font-size: 14px; color: #000; }

        .sidebar .authors-title { font-weight: 600; font-size: 21px; color: var(--head); margin: 0 0 28px; }
        .author { display: flex; align-items: center; gap: 18px; margin-bottom: 26px; }
        .avatar {
            flex: none; width: 80px; height: 80px; border-radius: 50%; background: var(--avatar);
            display: flex; align-items: center; justify-content: center;
            color: #000; font-weight: 600; font-size: 17px; letter-spacing: .5px;
        }
        .a-name { font-weight: 600; font-size: 17px; color: var(--head); }
        .a-org { font-weight: 300; font-size: 12px; color: var(--muted); margin-top: 6px; }

        .site-footer { background: var(--pastel); }
        .site-footer .container { padding-top: 28px; padding-bottom: 28px; text-align: center; }
        .site-footer p { margin: 0; font-size: 12px; color: #555; }
    </style>
</head>
<body>
    <header class="site-header">
        <div class="container header-inner">
            <img class="brand-mark" src="/img/andaliz-logo.svg" alt="Andaliz Publishing">
            <span class="brand-name">Andaliz Publishing</span>
        </div>
    </header>

    <main class="container layout">
        <article>
            <h1 class="title">Hydrostatic Equilibrium and the Formation of the Near-Spherical Structure of the Sun</h1>

            <div class="access-row">
                <div id="login"></div>
                <p class="alt-access">or <a href="#">Access and Purchase options</a></p>
            </div>

            <div class="tab">Abstract</div>
            <section class="abstract">
                <p>The Sun appears almost perfectly spherical because of the balance between gravity and internal pressure acting throughout the star. Gravity pulls solar material inward equally from all directions, while the pressure generated by nuclear fusion in the core pushes outward.</p>
                <p>This balance creates a shape that is close to a perfect sphere. Although the Sun rotates, which could cause flattening at the poles and expansion at the equator, its rotation speed is relatively slow compared to its size. As a result, the distortion is extremely small. This article examines the physical processes responsible for the Sun’s shape, including gravity, hydrostatic equilibrium, and rotational forces. The discussion also compares the Sun to other celestial bodies and explains why stars naturally tend toward spherical forms.</p>
            </section>

            <h2 class="section-title">Introduction</h2>
            <div class="body locked">
                <p>The Sun is the largest object in the Solar System and contains more than 99 percent of its total mass. When observed from Earth or from space-based telescopes, the Sun appears nearly perfectly round. This spherical shape is not accidental, but rather the result of fundamental physical forces acting inside the star.</p>
                <p>In astronomy, large celestial bodies tend to become spherical because gravity pulls matter inward equally in all directions. Once an object reaches sufficient mass, gravity overcomes the strength of the material composing the object and forces it into a rounded shape. This process is known as hydrostatic equilibrium. The Sun, being made mostly of hot plasma rather than solid material, is especially capable of forming a smooth and symmetric structure.</p>
                <p>However, the Sun is not mathematically perfect. Because it rotates on its axis, centrifugal forces slightly reduce the inward pull of gravity at the equator. This causes a very small equatorial bulge.</p>
            </div>

            <div class="cta">
                <p class="cta-title">Full article after sign-in at institution</p>
                <p>Check if you have access to the full article after sign-in.</p>
            </div>
        </article>

        <aside class="sidebar">
            <h2 class="authors-title">Authors</h2>
            <div class="author">
                <span class="avatar">HK</span>
                <div><div class="a-name">Authori Surna</div><div class="a-org">Prinsengracht University</div></div>
            </div>
            <div class="author">
                <span class="avatar">BRS</span>
                <div><div class="a-name">Bojana Ran Somro</div><div class="a-org">Prinsengracht University</div></div>
            </div>
        </aside>
    </main>

    <footer class="site-footer">
        <div class="container"><p>Demo by Seamless Access team</p></div>
    </footer>

    <script src="https://${SERVICE_HOST}/thiss.js"></script>
    <script>
        window.onload = function () {
            thiss.DiscoveryComponent({
                loginInitiatorURL: 'https://${SP1_HOST}/Shibboleth.sso/Login?target=https://${SP1_HOST}/secure/',
                entityID: '${SP1_ENTITYID}'
            }).render('#login');
        };
    </script>

    <!-- ============================================================
         Demo instructions overlay — implements the Figma "Advanced
         instructions" dialog (node 2543:10166) on its scrim (2543:10181).
         Brand-neutral and self-contained; the IDENTICAL block lives in
         sp2/html/index.html.tmpl — edit both together. Shown on every
         load; dismissable via the ✕, "Continue to demo", Esc, or the
         backdrop. (To show it only once per visitor, gate showOverlay()
         on localStorage — see the comment in the script below.)
         ============================================================ -->
    <style>
        .demo-overlay {
            position: fixed; inset: 0; z-index: 2000; padding: 24px;
            display: flex; align-items: center; justify-content: center;
            background: rgba(120, 120, 120, 0.43);
            font-family: Inter, system-ui, -apple-system, "Segoe UI", sans-serif;
        }
        .demo-overlay[hidden] { display: none; }
        .demo-overlay__card {
            width: 582px; max-width: 100%; max-height: calc(100vh - 48px);
            overflow-y: auto; background: #fff; border-radius: 20px;
            box-shadow: 0 5px 16px rgba(0, 0, 0, 0.28); padding: 24px;
            display: flex; flex-direction: column; gap: 24px;
        }
        .demo-overlay__head { display: flex; align-items: flex-start; gap: 24px; }
        .demo-overlay__title { flex: 1; margin: 0; font-weight: 700; font-size: 24px; line-height: 1.2; color: #000; }
        .demo-overlay__close {
            flex: none; width: 24px; height: 24px; padding: 0; border: 0;
            background: none; color: #000; cursor: pointer; line-height: 0;
        }
        .demo-overlay__close svg { display: block; }
        .demo-overlay__body { color: #535353; font-size: 14px; line-height: 22px; }
        .demo-overlay__body p { margin: 0 0 16px; }
        .demo-overlay__body ul { margin: 0 0 16px; padding-left: 21px; }
        .demo-overlay__body li { margin: 0; }
        .demo-overlay__body :last-child { margin-bottom: 0; }
        .demo-overlay__actions { display: flex; justify-content: flex-end; }
        .demo-overlay__continue {
            height: 44px; padding: 0 14px; border: 0; border-radius: 6px;
            background: #216e93; color: #fff; font-family: inherit; font-size: 14px; cursor: pointer;
        }
    </style>
    <div id="demo-overlay" class="demo-overlay" role="dialog" aria-modal="true" aria-labelledby="demo-overlay-title" hidden>
        <div class="demo-overlay__card">
            <div class="demo-overlay__head">
                <h2 id="demo-overlay-title" class="demo-overlay__title">Advanced instructions</h2>
                <button type="button" class="demo-overlay__close" aria-label="Close instructions">
                    <svg viewBox="0 0 24 24" width="24" height="24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><path d="M6 6l12 12M18 6L6 18"/></svg>
                </button>
            </div>
            <div class="demo-overlay__body">
                <p>Advanced integration enables content websites to provide a custom experience to its users. To use the demo, a few example identity providers (with login) are provided to allow you to evaluate the full experience.</p>
                <p>Available identity providers:</p>
                <ul>
                    <li>Demo IdP</li>
                    <li>University of Cazalla</li>
                </ul>
                <p>Login at identity provider:</p>
                <ul>
                    <li>Username: demo</li>
                    <li>Password: demopassword</li>
                </ul>
            </div>
            <div class="demo-overlay__actions">
                <button type="button" class="demo-overlay__continue">Continue to demo</button>
            </div>
        </div>
    </div>
    <script>
        (function () {
            var overlay = document.getElementById('demo-overlay');
            if (!overlay) return;
            // To show only once per visitor, wrap the reveal below in:
            //   if (localStorage.getItem('demoOverlayDismissed')) return;
            // and call localStorage.setItem('demoOverlayDismissed', '1') in hide().
            function hide() {
                overlay.setAttribute('hidden', '');
                document.removeEventListener('keydown', onKey);
            }
            function onKey(e) { if (e.key === 'Escape') hide(); }
            overlay.removeAttribute('hidden');
            document.addEventListener('keydown', onKey);
            overlay.addEventListener('click', function (e) { if (e.target === overlay) hide(); });
            overlay.querySelector('.demo-overlay__close').addEventListener('click', hide);
            overlay.querySelector('.demo-overlay__continue').addEventListener('click', hide);
        })();
    </script>
    <script src="/demo-toolbar/demo-toolbar.js" defer></script>
</body>
</html>
