<!DOCTYPE html>
<!-- Rendered from index.html.tmpl by scripts/render.sh. Public (unauthenticated)
     landing page for SP2 — "Prinsen". Abstract + truncated intro, with the
     SeamlessAccess button inside the "Authenticate for full-text" card. The blue
     button is rendered by thiss.js into #login — do not hardcode a look-alike. -->
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Prinsen — The Triaxial Shape and Surface Irregularities of Mars</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=IBM+Plex+Serif:wght@400;600;700&family=Libre+Franklin&family=Nunito+Sans:wght@400;600;700&family=Numans&family=Poppins:wght@400;600&family=Inter:wght@400;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --magenta: #731963; --teal: #0b3c49; --ink: #2a2a2a;
            --link: #0079ff; --border: #e3e3e3;
        }
        * { box-sizing: border-box; }
        body {
            margin: 0; color: var(--ink); background: #fff;
            font-family: "Nunito Sans", system-ui, sans-serif; line-height: 1.6;
        }
        .container { max-width: 1180px; margin: 0 auto; padding: 0 40px; }
        @media (max-width: 720px) { .container { padding: 0 20px; } }

        /* Top nav */
        .nav { position: sticky; top: 0; z-index: 10; background: rgba(255,255,255,.9); backdrop-filter: blur(14px); border-bottom: 3px solid var(--teal); }
        .nav-inner { display: flex; align-items: center; gap: 28px; height: 80px; }
        .brand { display: flex; align-items: center; gap: 12px; }
        .brand-mark { width: 26px; height: 30px; display: block; }
        .brand-name { font-family: Numans, system-ui, sans-serif; font-size: 18px; color: #000; }
        .nav-links { display: flex; gap: 40px; }
        .nav-links a { font-family: "Nunito Sans", sans-serif; font-weight: 600; font-size: 16px; color: var(--ink); text-decoration: none; }
        .nav-links a:first-child::before { content: "●"; color: var(--magenta); font-size: 9px; vertical-align: middle; margin-right: 8px; }
        .nav-icons { margin-left: auto; display: flex; gap: 22px; color: var(--ink); }
        .nav-icons svg { width: 22px; height: 22px; display: block; }
        @media (max-width: 820px) { .nav-links { display: none; } }

        /* Hero */
        .hero { background: var(--magenta); }
        .hero .container { padding-top: 54px; padding-bottom: 54px; }
        .hero h1 { font-family: "IBM Plex Serif", Georgia, serif; font-weight: 600; font-size: 35px; line-height: 1.25; color: #fff; margin: 0; max-width: 940px; }
        @media (max-width: 720px) { .hero h1 { font-size: 27px; } }

        /* Body layout */
        .layout { display: grid; grid-template-columns: minmax(0, 1fr) 350px; gap: 56px; padding-top: 40px; padding-bottom: 64px; }
        @media (max-width: 900px) { .layout { grid-template-columns: 1fr; gap: 40px; } }

        .authors { font-family: Poppins, sans-serif; font-size: 13px; color: var(--ink); opacity: .7; margin: 0 0 18px; }
        .h-abstract { font-family: "Nunito Sans", sans-serif; font-weight: 700; font-size: 18px; color: var(--ink); margin: 0 0 6px; }
        .body p { font-size: 16px; color: var(--ink); margin: 0 0 18px; }

        .auth-card { border: 1px solid var(--border); border-top: 4px solid var(--ink); border-radius: 4px; padding: 28px 36px 34px; margin-top: 28px; max-width: 705px; }
        .auth-card-title { font-family: "IBM Plex Serif", Georgia, serif; font-weight: 600; font-size: 18px; color: var(--ink); margin: 0 0 12px; }
        .auth-card-desc { font-size: 15px; color: var(--ink); margin: 0 0 24px; }
        #login { width: 350px; max-width: 100%; min-height: 58px; }

        /* Sidebar */
        .rec-title { font-family: Poppins, sans-serif; font-weight: 600; font-size: 18px; color: var(--ink); margin: 0 0 20px; }
        .rec-card { background: #fff; border-radius: 3px; box-shadow: 0 1px 6px rgba(0,0,0,.12); overflow: hidden; max-width: 350px; }
        .rec-card img { display: block; width: 100%; height: 200px; object-fit: cover; }
        .rec-body { padding: 18px 20px 22px; }
        .rec-link { font-family: "IBM Plex Serif", Georgia, serif; font-weight: 700; font-size: 18px; line-height: 1.4; color: var(--ink); text-decoration: underline; }
        .rec-meta { font-family: "IBM Plex Serif", Georgia, serif; font-size: 12px; color: var(--ink); margin: 10px 0 0; }

        /* Footer */
        .site-footer { background: var(--teal); }
        .site-footer .container { display: flex; align-items: center; gap: 20px; padding-top: 40px; padding-bottom: 40px; }
        .footer-mark { width: 26px; height: 30px; display: block; }
        .site-footer p { margin: 0; font-size: 12px; color: #eaeaea; }
    </style>
</head>
<body>
    <nav class="nav">
        <div class="container nav-inner">
            <span class="brand">
                <img class="brand-mark" src="/img/prinsen-logo.svg" alt="">
                <span class="brand-name">Prinsen</span>
            </span>
            <span class="nav-links">
                <a href="#">Journals</a><a href="#">Books</a><a href="#">Authors</a><a href="#">Conferences</a>
            </span>
            <span class="nav-icons">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="8" r="4"/><path d="M4 20c0-4 3.6-6 8-6s8 2 8 6"/></svg>
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="11" cy="11" r="7"/><path d="M21 21l-4-4"/></svg>
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M3 6h18M3 12h18M3 18h18"/></svg>
            </span>
        </div>
    </nav>

    <header class="hero">
        <div class="container">
            <h1>The Triaxial Shape and Surface Irregularities of Mars: Geological Processes and the Nerio Hypothesis</h1>
        </div>
    </header>

    <main class="container layout">
        <article>
            <p class="authors">Tianyi Hao; Alejandro Sánchez-Postigo; Pavel Cheben; Alejandro Ortega-Moñux;</p>

            <div class="body">
                <h2 class="h-abstract">Abstract</h2>
                <p>Mars differs significantly from a perfect sphere and is more accurately described as a triaxial ellipsoid with large-scale surface irregularities. Unlike Earth, whose stronger gravity smooths much of its surface into a more uniform shape, Mars displays substantial topographic variation caused by volcanic activity, crustal thickness differences, ancient impacts, and tectonic processes. These irregularities produce a planetary form often compared to a lumpy or imbalanced rugby ball.</p>
                <p>In addition to established geological explanations, some theoretical models propose that Mars may once have possessed an ancient moon, sometimes referred to hypothetically as Nerio, whose gravitational influence and eventual destruction could have contributed to crustal deformation and planetary imbalance. This article examines the physical and geological processes responsible for the shape of Mars, including rotational deformation, volcanic loading, hemispheric asymmetry, and speculative satellite interaction theories.</p>
            </div>

            <section class="auth-card">
                <h2 class="auth-card-title">Authenticate for full-text</h2>
                <p class="auth-card-desc">Authenticate using your preferred authentication provider to gain access to articles from the Dual-Band journal and more at Prinsen publishing.</p>
                <div id="login"></div>
            </section>
        </article>

        <aside class="sidebar">
            <h2 class="rec-title">Recommended for you</h2>
            <div class="rec-card">
                <img src="/img/recommended.jpg" alt="Saturn’s rings">
                <div class="rec-body">
                    <a class="rec-link" href="#">Gravitational Perturbations and the Irregular Geometry of Saturn’s Rings</a>
                    <p class="rec-meta">2026 · Journal Saturn · James Bojanda</p>
                </div>
            </div>
        </aside>
    </main>

    <footer class="site-footer">
        <div class="container">
            <img class="footer-mark" src="/img/prinsen-logo-white.svg" alt="">
            <p>Demo by Seamless Access team</p>
        </div>
    </footer>

    <script src="https://${SERVICE_HOST}/thiss.js"></script>
    <script>
        window.onload = function () {
            thiss.DiscoveryComponent({
                loginInitiatorURL: 'https://${SP2_HOST}/Shibboleth.sso/Login?target=https://${SP2_HOST}/secure/',
                entityID: '${SP2_ENTITYID}'
            }).render('#login');
        };
    </script>

    <!-- ============================================================
         Demo instructions overlay — implements the Figma "Advanced
         instructions" dialog (node 2543:10166) on its scrim (2543:10181).
         Brand-neutral and self-contained; the IDENTICAL block lives in
         sp1/html/index.html.tmpl — edit both together. Shown on every
         load; dismissable via the ✕, "Continue to demo", Esc, or the
         backdrop. (To show it only once per visitor, gate showOverlay()
         on localStorage — see the comment in the script below.)
         ============================================================ -->
    <style>
        .demo-overlay {
            position: fixed; inset: 0; z-index: 2000; padding: 24px;
            display: flex; align-items: center; justify-content: center;
            background: rgba(120, 120, 120, 0.43);
            font-family: Inter, system-ui, -apple-system, "Segoe UI", sans-serif;
        }
        .demo-overlay[hidden] { display: none; }
        .demo-overlay__card {
            width: 582px; max-width: 100%; max-height: calc(100vh - 48px);
            overflow-y: auto; background: #fff; border-radius: 20px;
            box-shadow: 0 5px 16px rgba(0, 0, 0, 0.28); padding: 24px;
            display: flex; flex-direction: column; gap: 24px;
        }
        .demo-overlay__head { display: flex; align-items: flex-start; gap: 24px; }
        .demo-overlay__title { flex: 1; margin: 0; font-weight: 700; font-size: 24px; line-height: 1.2; color: #000; }
        .demo-overlay__close {
            flex: none; width: 24px; height: 24px; padding: 0; border: 0;
            background: none; color: #000; cursor: pointer; line-height: 0;
        }
        .demo-overlay__close svg { display: block; }
        .demo-overlay__body { color: #535353; font-size: 14px; line-height: 22px; }
        .demo-overlay__body p { margin: 0 0 16px; }
        .demo-overlay__body ul { margin: 0 0 16px; padding-left: 21px; }
        .demo-overlay__body li { margin: 0; }
        .demo-overlay__body :last-child { margin-bottom: 0; }
        .demo-overlay__actions { display: flex; justify-content: flex-end; }
        .demo-overlay__continue {
            height: 44px; padding: 0 14px; border: 0; border-radius: 6px;
            background: #216e93; color: #fff; font-family: inherit; font-size: 14px; cursor: pointer;
        }
    </style>
    <div id="demo-overlay" class="demo-overlay" role="dialog" aria-modal="true" aria-labelledby="demo-overlay-title" hidden>
        <div class="demo-overlay__card">
            <div class="demo-overlay__head">
                <h2 id="demo-overlay-title" class="demo-overlay__title">Advanced instructions</h2>
                <button type="button" class="demo-overlay__close" aria-label="Close instructions">
                    <svg viewBox="0 0 24 24" width="24" height="24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><path d="M6 6l12 12M18 6L6 18"/></svg>
                </button>
            </div>
            <div class="demo-overlay__body">
                <p>Advanced integration enables content websites to provide a custom experience to its users. To use the demo, a few example identity providers (with login) are provided to allow you to evaluate the full experience.</p>
                <p>Available identity providers:</p>
                <ul>
                    <li>Demo IdP</li>
                    <li>University of Cazalla</li>
                </ul>
                <p>Login at identity provider:</p>
                <ul>
                    <li>Username: demo</li>
                    <li>Password: demopassword</li>
                </ul>
            </div>
            <div class="demo-overlay__actions">
                <button type="button" class="demo-overlay__continue">Continue to demo</button>
            </div>
        </div>
    </div>
    <script>
        (function () {
            var overlay = document.getElementById('demo-overlay');
            if (!overlay) return;
            // To show only once per visitor, wrap the reveal below in:
            //   if (localStorage.getItem('demoOverlayDismissed')) return;
            // and call localStorage.setItem('demoOverlayDismissed', '1') in hide().
            function hide() {
                overlay.setAttribute('hidden', '');
                document.removeEventListener('keydown', onKey);
            }
            function onKey(e) { if (e.key === 'Escape') hide(); }
            overlay.removeAttribute('hidden');
            document.addEventListener('keydown', onKey);
            overlay.addEventListener('click', function (e) { if (e.target === overlay) hide(); });
            overlay.querySelector('.demo-overlay__close').addEventListener('click', hide);
            overlay.querySelector('.demo-overlay__continue').addEventListener('click', hide);
        })();
    </script>
</body>
</html>
